#include <stdio.h>

int main(){
    int a,b;

    printf("Pick first number from 1 to 9: "\n);
    scanf("%d", &a);

    printf("Pick second number from 1 to 9: "\n);
    scanf("%d", &b);
return 0;
}
